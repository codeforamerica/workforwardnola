'use strict';

var answers = {};

function finishAssessment() {
  var $answers = $('input:checked');
  console.log($answers);
  // TODO if $answers.length != the # of images somehow indicate you're not done
  
}
